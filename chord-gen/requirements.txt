# PyTorch — for training and inference.
# For GPU training install the CUDA-enabled wheel instead:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# See https://pytorch.org/get-started/locally/ for the right URL for your CUDA version.
torch>=2.1

# Numerical computing
numpy>=1.24

# Dataset preparation
pandas>=2.0
pyarrow>=13.0        # parquet engine for pandas

# HuggingFace dataset streaming (registers the hf:// fsspec protocol used by clean_chordonomicon.py)
huggingface_hub[hf_transfer]>=0.20
fsspec>=2024.2
